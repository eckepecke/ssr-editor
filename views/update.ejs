<%- include('includes/header'); %>
    <h2>Dokument</h2>
    <form method="POST" action="/update/<%= doc ? doc.id : '' %>" class="new-doc">
        <input type="hidden" name="id" value="<%= doc ? doc.id : '' %>" />

        <label for="title">Titel</label>
        <input type="text" name="title" value="<%= doc ? doc.title : '' %>" />

        <label for="content">Innehåll</label>
        <textarea name="content"><%= doc && doc.content ? doc.content : '' %></textarea>

        <input type="submit" value="Uppdatera" />
    </form>
<%- include('includes/footer'); %>
